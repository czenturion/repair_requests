version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./public:/app/public
      - db_data:/app/data
    environment:
      - NODE_ENV=production

volumes:
  db_data: